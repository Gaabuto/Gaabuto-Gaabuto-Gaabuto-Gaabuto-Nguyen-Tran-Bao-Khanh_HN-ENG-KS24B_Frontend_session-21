for (let i = 0; i < 10; i++) {
    let color = ['red', 'orange', 'green', 'blue', 'pink',];
    let random = color[Math.floor(Math.random() * color.length)];
    console.log('Hello World', 'color: ' + random);


}